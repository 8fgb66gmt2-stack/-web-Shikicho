const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('.global-nav');if(menu){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open);menu.textContent=open?'閉じる':'メニュー'})}document.querySelectorAll('.global-nav a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');if(menu){menu.setAttribute('aria-expanded','false');menu.textContent='メニュー'}}));const updated=document.getElementById('last-updated');if(updated){const d=new Date();const y=d.getFullYear();const m=String(d.getMonth()+1).padStart(2,'0');const day=String(d.getDate()).padStart(2,'0');updated.textContent=`最終更新：玲瓏暦 ${y}年${m}月${day}日`}
